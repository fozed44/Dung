#pragma once

#include "TextOverlayWindow.h"
#include "TextSheet.h"

using namespace mp::gfx;

void TextOverlayWindow::Render(const IScene* pScene) {
	/*
	for (auto s : pScene->._stringOverlays) {
		SDL_Point targetLocation{
			s._location.x,
			s._location.y
		};

		_pTextSheet->RenderString(GetRenderer(), s._s, targetLocation);
	}
	*/
}

